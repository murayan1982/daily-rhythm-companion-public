"""Application version metadata for the active Daily Rhythm Companion source.

Published v2.x/v3.x artifacts remain immutable. This constant identifies the
active v4.0.0 release-candidate source and is reused by FastAPI and runtime
status output. The current published release remains v3.0.0; Control B does not
publish v4.0.0.
"""

APP_VERSION = "4.0.0"
